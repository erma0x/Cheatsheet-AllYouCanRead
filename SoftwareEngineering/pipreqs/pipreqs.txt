pip install pipreqs

pipreqs /path/to/project   --force

# generate requirements.txt of the project
# is better than pip freeze > requirements.txt
# because capture the essential packages of your project
